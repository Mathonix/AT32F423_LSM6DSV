import { PageHeader } from "@/components/common/page-header"; import { useDevice } from "@/context/device-context";
export function CanPage(){const {settings,updateSettings}=useDevice();const input="h-8 rounded-md border border-border bg-input px-2 text-xs";return <div className="min-h-full"><PageHeader title="CAN ??" description="?? CAN ?????????????"/><div className="space-y-4 p-5"><section className="rounded-lg border border-border bg-surface-1"><div className="border-b border-border px-4 py-3 text-xs font-medium">CAN ????</div><div className="grid grid-cols-3 gap-4 p-4 text-xs"><label className="space-y-1.5"><span className="text-muted-foreground">?????</span><select className={input} value={settings.canBitrate} onChange={e=>updateSettings({canBitrate:Number(e.target.value)})}>{[125000,250000,500000,1000000].map(v=><option key={v} value={v}>{v.toLocaleString()} bit/s</option>)}</select></label><label className="space-y-1.5"><span className="text-muted-foreground">????</span><input className={input} type="number" min={1} max={1000} value={settings.outputHz} onChange={e=>updateSettings({outputHz:Number(e.target.value)})}/></label><label className="flex items-end gap-2 pb-1"><input type="checkbox"/>?? CAN ??</label></div><div className="px-4 pb-4 text-[11px] text-warning">CAN ?????????????????????????????????????????</div></section><section className="grid grid-cols-4 gap-px overflow-hidden rounded-lg border border-border bg-border text-xs">{[['CAN ??','???'],['???',`${settings.canBitrate.toLocaleString()} bit/s`],['????',`${settings.outputHz} Hz`],['????','JustFloat????']].map(([a,b])=><div key={a} className="bg-surface-1 p-4"><div className="text-muted-foreground">{a}</div><div className="mt-1 font-mono">{b}</div></div>)}</section></div></div>}
